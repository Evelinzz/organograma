import './Banner.css'

function Banner() {
    return (
    //JSX, permite uma escrita mais proxima ao HTML para manipular elementos DOM
    <header className="banner">
        <img src="/imagens/banner.png" alt="O banner principal da pagina do Organo"/>
    </header>    
    )
}

export default Banner